# Secrets
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo
*.bak
venv/
.venv/
*.egg-info/

# OS
.DS_Store

# Test data variants (we only ship locomo10.json)
data/locomo_single.json

# NOTE: results/ and charts/ are intentionally NOT ignored.
# We commit the April 29, 2026 baseline once so readers can verify
# the numbers in the README without re-running the benchmark.
# Future runs will overwrite locally; commit new baselines deliberately.
